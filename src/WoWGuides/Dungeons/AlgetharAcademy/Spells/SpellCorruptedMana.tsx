import { FunctionComponent } from 'react';

import Spell from '../../../Tags/Components/Spell';

const SpellCorruptedMana: FunctionComponent = () => (
    <Spell id={386201}>Corrupted Mana</Spell>
);

export default SpellCorruptedMana;
