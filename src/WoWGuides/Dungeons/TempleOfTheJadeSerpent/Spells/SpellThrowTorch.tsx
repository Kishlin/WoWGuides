import { FunctionComponent } from 'react';

import Spell from '../../../Tags/Components/Spell';

const SpellThrowTorch: FunctionComponent = () => (
    <Spell id={114803}>Throw Torch</Spell>
);

export default SpellThrowTorch;
