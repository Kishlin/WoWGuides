import { FunctionComponent } from 'react';

import Spell from '../../../Tags/Components/Spell';

const SpellChargedPulse: FunctionComponent = () => (
    <Spell id={210875}>Charged Pulse</Spell>
);

export default SpellChargedPulse;
